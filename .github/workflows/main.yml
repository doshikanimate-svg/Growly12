name: Keep Render Awake

on:
  schedule:
    - cron: "*/14 * * * *" # Каждые 14 минут
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Service
        run: curl -s https://growly12.onrender.com/api/health > /dev/null
